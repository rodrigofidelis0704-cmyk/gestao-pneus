import {AppShell} from '@/components/AppShell';import {appliedTires} from '@/lib/demoData';import {classificarSulco} from '@/lib/domain/inspecao';
export default function Aplicados(){return <AppShell><div className="content"><div className="pageHead"><div><span className="eyebrow">GESTÃO</span><h2>Pneus aplicados</h2><p>Todos os pneus atualmente montados na frota.</p></div></div><div className="kpis"><div className="kpi"><small>Aplicados</small><b>{appliedTires.length}</b></div><div className="kpi danger"><small>Troca imediata</small><b>{appliedTires.filter(t=>t.mm<=3.5).length}</b></div><div className="kpi warning"><small>Inspeção em 7 dias</small><b>{appliedTires.filter(t=>t.mm>3.5&&t.mm<=5).length}</b></div><div className="kpi orange"><small>Inspeção em 15 dias</small><b>{appliedTires.filter(t=>t.mm>5&&t.mm<=7).length}</b></div></div><div className="section"><table className="table"><thead><tr><th>Nº fogo</th><th>Material</th><th>Marca / modelo</th><th>Medida</th><th>Vida</th><th>Equipamento</th><th>Placa</th><th>Posição</th><th>Sulco</th><th>Situação</th></tr></thead><tbody>{appliedTires.map(t=>{const c=classificarSulco(t.mm);return <tr key={t.fogo}><td><b>{t.fogo}</b></td><td>{t.material} · {t.descricao}</td><td>{t.marca} {t.modelo}</td><td>{t.medida}</td><td>{t.vida}</td><td>{t.equipamento}</td><td>{t.placa}</td><td><b>{t.posicao}</b></td><td>{t.mm.toFixed(1)} mm</td><td><span className={'pill '+c.status.toLowerCase()}>{c.texto}</span></td></tr>})}</tbody></table></div></div></AppShell>}
